{
  "name": "mcp-meta-hub",
  "version": "0.1.11",
  "description": "A lightweight MCP proxy that aggregates multiple MCP servers into two meta-tools: list_tools and call_tool. Stop giving your AI agent a shell — give it skills.",
  "type": "module",
  "bin": {
    "mcp-meta-hub": "dist/index.js"
  },
  "main": "dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsup",
    "build:test": "tsup src/index.ts src/e2e.test.ts",
    "dev": "tsup --watch",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "tsup src/index.ts src/e2e.test.ts src/config.test.ts src/resilience.test.ts src/features.test.ts --format esm --clean --sourcemap --target node20 --no-dts && node dist/config.test.js && node dist/features.test.js && node dist/e2e.test.js && node dist/resilience.test.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "proxy",
    "aggregator",
    "ai-tools",
    "llm",
    "skills"
  ],
  "author": "Luc Verdier",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/verdier/mcp-meta-hub.git"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.4.0",
    "typescript": "^5.7.0"
  }
}
